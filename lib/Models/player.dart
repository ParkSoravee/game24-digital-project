class Player {
  String name;
  int score;
  bool isActive;
  bool isAnswer;

  Player({
    required this.name,
    required this.score,
    required this.isActive,
    required this.isAnswer,
  });
}
