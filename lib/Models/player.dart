class Player {
  String key;
  String name;
  int score;
  bool isActive;
  bool isAnswer;

  Player({
    required this.key,
    required this.name,
    required this.score,
    required this.isActive,
    required this.isAnswer,
  });
}
